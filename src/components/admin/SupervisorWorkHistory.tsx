export default function SupervisorWorkHistory({supervisor,trainees,activities,sessions,minutes,documents}:{supervisor:any;trainees:any[];activities:any[];sessions:any[];minutes:any[];documents:any[]}){
  const name=(id:string)=>trainees.find(t=>t.id===id)?.name||"—";
  const rows=[...sessions.map(x=>({...x,kind:"جلسة",when:x.date||x.createdAt})),...minutes.map(x=>({...x,kind:"محضر اجتماع",when:x.date||x.createdAt})),...activities.filter(x=>String(x.activityType||"").startsWith("supervision_")).map(x=>({...x,kind:"ساعات إشراف",when:x.date||x.createdAt})),...documents.map(x=>({...x,kind:"مستند",when:x.issuedAt||x.createdAt}))].sort((a,b)=>String(b.when||"").localeCompare(String(a.when||"")));
  return <section dir="rtl" style={{marginBottom:18}}><div style={{background:"linear-gradient(125deg,#001442,#0D40FC)",color:"white",borderRadius:18,padding:"20px 24px",marginBottom:14,display:"flex",justifyContent:"space-between",alignItems:"center",gap:12,flexWrap:"wrap"}}><div><small style={{color:"#8FE8FF"}}>الملف الرقابي للمشرف</small><h2 style={{margin:"5px 0"}}>{supervisor.name}</h2><span style={{color:"#D6E1FF",fontSize:12}}>{supervisor.email}</span></div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><a href="#work-log" style={{color:"white",textDecoration:"none",padding:"8px 12px",border:"1px solid #ffffff40",borderRadius:9,fontSize:12}}>سجل العمل</a><a href="#trainee-list" style={{color:"white",textDecoration:"none",padding:"8px 12px",border:"1px solid #ffffff40",borderRadius:9,fontSize:12}}>المتدربون</a><a href="#schedule" style={{color:"#001442",background:"white",textDecoration:"none",padding:"8px 12px",borderRadius:9,fontSize:12,fontWeight:700}}>المواعيد والمقاعد</a></div></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:12,marginBottom:14}}>{[["المتدربون الحاليون",trainees.length,"#0D40FC"],["الجلسات المسجلة",sessions.length,"#7C3AED"],["محاضر الاجتماعات",minutes.length,"#D97706"],["ساعات الإشراف المعتمدة",activities.filter(a=>a.status==="approved"&&String(a.activityType||"").startsWith("supervision_")).reduce((n,a)=>n+Number(a.duration||0),0),"#059669"],["المستندات",documents.length,"#0891B2"]].map(([l,v,c])=><div key={String(l)} style={{background:"white",border:"1px solid #E2E8F0",borderTop:`3px solid ${c}`,borderRadius:14,padding:17}}><b style={{fontSize:25,color:String(c)}}>{String(v)}</b><div style={{fontSize:13,color:"#64748B"}}>{String(l)}</div></div>)}</div><div id="trainee-list" style={{background:"white",border:"1px solid #E2E8F0",borderRadius:16,padding:18,marginBottom:14,scrollMarginTop:20}}><h3 style={{margin:"0 0 13px",color:"#001442"}}>المتدربون المرتبطون بالمشرف</h3><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:9}}>{trainees.map(t=><a key={t.id} href={`/ar/admin/trainees/${t.id}`} style={{padding:12,border:"1px solid #E2E8F0",borderRadius:11,textDecoration:"none",color:"#001442",fontWeight:700}}>{t.name}<small style={{display:"block",color:"#64748B",fontWeight:400,marginTop:4}}>{t.email||t.license||"فتح ملف المتدرب"}</small></a>)}{!trainees.length&&<span style={{color:"#64748B",fontSize:13}}>لا يوجد متدربون مرتبطون حاليًا.</span>}</div></div><div id="work-log" style={{background:"white",border:"1px solid #E2E8F0",borderRadius:16,overflowX:"auto",scrollMarginTop:20}}><h3 style={{padding:"18px 20px",margin:0,color:"#001442"}}>كل ما نفذه المشرف مع المتدربين</h3><table style={{width:"100%",borderCollapse:"collapse",minWidth:760,fontSize:14}}><thead><tr style={{background:"#F8FAFC"}}>{["التاريخ","النوع","المتدرب","المدة/الحالة","التفاصيل"].map(h=><th key={h} style={{padding:13,textAlign:"right",color:"#475569",fontSize:13}}>{h}</th>)}</tr></thead><tbody>{rows.slice(0,300).map((row:any,index)=><tr key={`${row.kind}-${row.id||index}`} style={{borderTop:"1px solid #EEF2F7"}}><td style={{padding:13}}>{String(row.when||"—").slice(0,10)}</td><td style={{padding:13,fontWeight:700}}>{row.kind}</td><td style={{padding:13}}>{name(row.traineeId)||(row.traineeIds||[]).map(name).join("، ")}</td><td style={{padding:13}}>{row.duration?`${row.duration} ساعة`:row.status||row.type||"—"}</td><td style={{padding:13,maxWidth:420}}>{row.description||row.discussion||row.title||row.notes||"—"}</td></tr>)}</tbody></table>{!rows.length&&<p style={{padding:30,textAlign:"center",color:"#64748B"}}>لا توجد أعمال مسجلة لهذا المشرف حتى الآن.</p>}</div></section>;
}
